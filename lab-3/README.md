# Лабораторная №3

`Вариант 9999`

Selenium IDE-шаблон: `tests/statcounter.side`.

Текущий набор проверяется только в Google Chrome:

```bash
google-chrome --version
```

В проверенном стенде использовался `Google Chrome 145.0.7632.75` и разрешение
`1280x800`.

Для Selenium IDE импортируйте `tests/statcounter.side` и запустите suite
`Statcounter Chrome UI suite`.

Для Selenium RC подготовлен Chrome-only пример:

```text
tests/selenium-rc/StatcounterChromeRcTest.java
```
